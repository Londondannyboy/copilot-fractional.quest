// Onboarding Wizard - Removed CopilotKit dependency
// This component is now a placeholder as AI features have been removed

interface OnboardingWizardProps {
  onComplete?: () => void;
}

export function OnboardingWizard({ onComplete }: OnboardingWizardProps) {
  return null;
}
